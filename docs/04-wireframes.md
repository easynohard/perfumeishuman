# 4단계: 와이어프레임

> 프로젝트: **perfumeishuman — "향수가 사람이라면?"**
> 작성일: 2026-08-14 · 상태: **확정 (2026-08-14 사용자 승인)**
> 근거 문서: [02-user-stories.md](./02-user-stories.md) · [03-sitemap-ia.md](./03-sitemap-ia.md) · 관련 ADR: [0006](./adr/0006-tag-vocabulary-and-answer-structure.md)
> 화면 요소 옆 괄호는 2단계 스토리·AC 매핑

---

## 0. 읽는 법 · 공통 규칙

- 모든 화면은 **모바일 360px 폭 기준**(N1). 관리자 3종만 데스크톱 우선.
- 박스 다이어그램은 저해상도 구조 정의다 — 색·타이포·아이콘·간격은 8단계에서 확정한다.
- 모든 탭 가능한 요소는 터치 타깃 44px 이상(N1). 본문 텍스트 16px 이상(N7).
- `┃ … ┃` 표시는 **하단 고정(sticky) 요소**, `┈┈┈` 은 카드 내부 구분선, `╌╌╌` 은 MVP 비노출 예약 슬롯.

### 컨셉 언어 (확정)

UI 문구는 "사람 소개하듯 말하는" 톤으로 통일한다 (1단계 §4.4).

| 위치 | 일반적 표현 | 이 서비스의 표현 |
|---|---|---|
| 향수 상세의 시향기 목록 제목 | 리뷰 N개 | **{향수 이름}(이)가 사람이라면 (N)** |
| 시향기 상세의 태그 답변 영역 | 태그 | **이 사람의 프로필** |
| 시향기 상세 하단 목록 | 같은 향수 다른 리뷰 | **이 향수의 다른 페르소나 (N)** |
| 작성 폼 등록 버튼 | 등록 | **내가 아는 {향수 이름}은/는 이래요** |
| 향수 상세의 스펙 정보(계열·노트) | 상세 정보 | **필요없는 정보들** (접힘 버튼) |
| 시향기 0개 빈 상태 | 아직 리뷰가 없어요 | **이 향수에 영혼을 불어넣어 사람으로 만들어주세요** |
| 본인 글 추천 버튼 자리 | (비활성) | **내가 소개한 사람이에요** |

- `{향수 이름}` 자리에는 실제 향수명이 들어간다. 한국어 조사((이)가 · 은/는)는 향수명의 받침 유무에 따라 자동 처리한다 — 구현 규칙은 10단계 코딩 컨벤션에서.

---

## 1. 공통 컴포넌트

### 1.1 상단 헤더

```
┌──────────────────────────────┐
│ perfumeishuman          [🔍] │   홈 3탭: 로고(=홈) + 검색 아이콘
│ ←                       [🔍] │   하위 화면: 뒤로가기 + 검색 아이콘
└──────────────────────────────┘
```

- 검색 아이콘은 모든 화면 상단에 있고(A1), 탭하면 전체 화면 검색 오버레이 `/search` (3단계 결정 4).
- 시향기 상세만 `[⋯]` 메뉴가 추가된다 (§2.7).

### 1.2 하단 탭바 (모바일 전 화면 공통)

```
┌──────────────────────────────┐
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- **쓰기가 중앙 강조 탭** — 서비스의 심장(N3, 3단계 결정 1). 시각적 강조(원형 버튼 등)는 8단계에서.
- 쓰기 탭: 멤버 → `/write` (향수 선택부터), 게스트 → 로그인 바텀시트(§3).
- MY 탭: 멤버 → `/me`, 게스트 → `/me`의 게스트 상태(로그인 버튼 노출, §4.2).
- 관리자 화면과 검색 오버레이에는 탭바 없음.

### 1.3 시향기 카드 (피드·목록 공용 컴포넌트)

```
┌──────────────────────────────┐
│ 딥티크 · 오 로즈              │  ← ⓐ 브랜드 · 향수명 (맨 위 — 확정)
│ ┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈┈ │
│ “첫 출근날 긴장한 신입사원    │  ← ⓑ 자유 서술 전문
│  같은 향이에요. 단정한데      │     (≤100자, 잘림 없음 — B1)
│  손끝은 떨리는.”             │
│                              │
│ 20대 · 차분한 · 출근          │  ← ⓒ 대표 태그 (질문 4종 중
│ 수줍은베르가못        ♥ 128   │     3개 질문에서 1개씩)
└──────────────────────────────┘  ← ⓓ 작성자 닉네임 · 추천 수
```

- 카드 전체가 탭 영역 → 시향기 상세 (B1). 1열 레이아웃.
- **목록은 10개씩 노출 + [더보기] 버튼** — 무한 스크롤 아님 (§6-10). 시향기 카드가 나오는 모든 목록(홈 피드·향수 상세·작성자 프로필·MY·시향기 상세 하단)에 공통 적용.
- **변형 A (기본)**: 위 그대로 — 홈 피드, 작성자 프로필, MY.
- **변형 B (같은 향수 문맥)**: ⓐ 브랜드·향수명 줄 생략 — 향수 상세의 목록, 시향기 상세 하단의 "이 향수의 다른 페르소나" (같은 향수라 중복 정보이므로).
- 대표 태그는 나이·성격·만나는 상황에서 각 1개 (어울리는 사람은 카드에서 생략 — 카드 무게 관리, 확정).

### 1.4 로그인 게이트 트리거 규칙 (화면 아님)

| 트리거 | 게스트일 때 | 로그인 완료 후 복귀 (C1) |
|---|---|---|
| 시향기 쓰기 (향수 상세 고정 바 · 쓰기 탭 · 빈 상태 버튼) | 바텀시트 | 작성 폼으로 — 작성 중이던 내용 복원 (D1) |
| 추천 버튼 | 바텀시트 | 그 시향기로 복귀 + **추천 자동 반영** (C2, F4) |
| MY 탭의 로그인 버튼 | 바텀시트 | `/me` 멤버 상태 |

멤버는 게이트 없이 그대로 통과. 열람 경로에는 어떤 로그인 벽도 없다 (N2).

---

## 2. 게스트 열람 가능 화면 (7)

### 2.1 홈 — 피드 탭 `/` (B1 · M7)

```
┌──────────────────────────────┐
│ perfumeishuman          [🔍] │
├──────────────────────────────┤
│  피드  브랜드로 찾기  계열로 찾기│ ← 상단 탭 3개, 활성 탭 밑줄
│  ────                        │    라벨 확정 (§6-1)
├──────────────────────────────┤
│ ┌──────────────────────────┐ │
│ │ 시향기 카드 (변형 A)       │ │ ← 추천순 고정 — 정렬 토글
│ └──────────────────────────┘ │    없음 (§6-2, B1 일부 변경)
│ ┌──────────────────────────┐ │
│ │ 시향기 카드 …             │ │ ← 10개씩 노출
│ └──────────────────────────┘ │
│         [ 더보기 ]           │ ← 탭 시 다음 10개 (§6-10)
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- 각 탭은 고유 URL(`/` · `/brands` · `/notes`)을 가진다 — 공유·SEO (3단계 결정 2).
- 첫 화면 = 즉시 읽을거리 (M7). 첫 카드가 FCP 안에 보이는 것이 N5의 대상.

### 2.2 홈 — 브랜드로 찾기 탭 `/brands` (A2)

```
┌──────────────────────────────┐
│ perfumeishuman          [🔍] │
├──────────────────────────────┤
│  피드  브랜드로 찾기  계열로 찾기│
│        ──────────            │
├──────────────────────────────┤
│  ㄱ                          │ ← 가나다/알파벳 섹션 헤더
│  ▢ 구찌              향수 12 │ ← 로고 · 브랜드명 · 향수 수
│  ▢ 겔랑              향수 24 │    행 전체 탭 → 브랜드 상세
│  ㄷ                          │
│  ▢ 딥티크            향수 18 │
│             ⋮                │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

### 2.3 홈 — 계열로 찾기 탭 `/notes` (A3)

```
┌──────────────────────────────┐
│ perfumeishuman          [🔍] │
├──────────────────────────────┤
│  피드  브랜드로 찾기  계열로 찾기│
│                    ─────────  │
├──────────────────────────────┤
│ ┌────────────┐┌────────────┐ │
│ │  시트러스   ││  플로럴     │ │ ← 계열 카드 2열 그리드
│ │  향수 34   ││  향수 52    │ │    탭 → 계열 상세
│ └────────────┘└────────────┘ │
│ ┌────────────┐┌────────────┐ │
│ │  우디      ││  머스크     │ │
│ │  향수 41   ││  향수 27    │ │
│ └────────────┘└────────────┘ │
│             ⋮                │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- 계열은 관리자 지정 통제 어휘 (A3, ADR 0003). 계열 수가 적어(10±) 그리드 한두 화면에 다 들어간다.

### 2.4 검색 `/search` (A1 · M1)

전체 화면 오버레이 — 어느 화면에서든 검색 아이콘으로 진입, URL 유지 (3단계 결정 4).

```
┌──────────────────────────────┐
│ ← ┌────────────────────────┐ │
│   │ 🔍 향수·브랜드 검색     │ │ ← 진입 즉시 키보드 포커스
│   └────────────────────────┘ │
├──────────────────────────────┤
│ ─ 입력 전: 인기 향수 ─        │
│  ▢ 오 로즈 · 딥티크  시향기 12│
│  ▢ 블랑쉬 · 바이레도 시향기 9 │
│             ⋮                │
│                              │
│ ─ 입력 중: 결과 (부분일치) ─  │
│  향수                        │
│  ▢ 오 로즈 · 딥티크  시향기 12│ ← 이미지·향수명·브랜드·시향기 수
│  브랜드                      │
│  ▢ 딥티크            향수 18 │
│                              │
│ ─ 결과 없음 ─                │
│  “아직 등록되지 않은          │
│   향수예요”                  │
│  + 인기 향수 목록 (위와 동일)  │ ← 빈 화면으로 끝내지 않음 (A1)
└──────────────────────────────┘
```

- 향수 결과 탭 → 향수 상세, 브랜드 결과 탭 → 브랜드 상세. 한글/영문 이름 모두 검색 대상 (A1).
- 하단 탭바 없음 (오버레이 — `←` 로 직전 화면 복귀).

### 2.5 브랜드 상세 `/brands/:slug` (A2)

```
┌──────────────────────────────┐
│ ←                       [🔍] │
├──────────────────────────────┤
│  ▢ 딥티크                    │ ← 로고 · 브랜드명
│  향수 18                     │
├──────────────────────────────┤
│  ▢ 오 로즈                   │
│    시향기 12 · ♥ 340         │ ← 시향기 수 · 총 추천 수 (A2)
│  ▢ 플레르 드 뽀              │    행 탭 → 향수 상세
│    시향기 8 · ♥ 210          │
│             ⋮                │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

### 2.6 계열 상세 `/notes/:slug` (A3)

브랜드 상세와 **대칭 구조** (3단계 결정 2) — 헤더만 계열명으로 교체.

```
┌──────────────────────────────┐
│ ←                       [🔍] │
├──────────────────────────────┤
│  시트러스                    │ ← 계열명
│  향수 34                     │
├──────────────────────────────┤
│  ▢ 오 드 뚜왈렛 A · 브랜드 X  │
│    시향기 12 · ♥ 340         │ ← 브랜드 상세와 동일한 행 구조.
│             ⋮                │    계열 문맥이라 브랜드명 병기
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

### 2.7 향수 상세 `/perfumes/:slug` (B2 · M2)

```
┌──────────────────────────────┐
│ ←                       [🔍] │
├──────────────────────────────┤
│        ┌────────┐            │
│        │ 이미지  │            │
│        └────────┘            │
│  오 로즈                      │ ← 향수명
│  딥티크 ›                    │ ← 브랜드 (→ 브랜드 상세)
│                              │
│  [ 필요없는 정보들 ▾ ]        │ ← 기본 접힘 (§6-4). 펼치면:
│  ┌─ 펼침 ──────────────────┐ │
│  │ 계열: [플로럴] [머스크]   │ │ ← 계열 칩 (→ 계열 상세)
│  │ 노트: 장미, 리치, 머스크  │ │ ← 있을 때만 (B2)
│  │ ╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌╌ │ │
│  │ (외부 구매 링크 슬롯 —    │ │ ← N12: 자리만 예약, MVP 비노출
│  │  MVP 비노출)             │ │
│  └─────────────────────────┘ │
├──────────────────────────────┤
│  오 로즈가 사람이라면 (12)     │ ← 버튼 바로 아래 시향기 목록
│  [추천순 ▾]  최신순           │ ← 여기는 정렬 토글 유지 (B2)
│ ┌──────────────────────────┐ │
│ │ 시향기 카드 (변형 B)       │ │ ← 10개씩 + [더보기] (§1.3)
│ └──────────────────────────┘ │
│         [ 더보기 ]           │
│                              │
│  ─ 시향기 0개일 때 ─          │
│   “이 향수에 영혼을 불어넣어  │
│    사람으로 만들어주세요”     │ ← 빈 상태 = 작성 유도 (B2)
├──────────────────────────────┤
│ ┃  ✍ 이 향수의 시향기 쓰기  ┃ │ ← 하단 고정 바 (§6-5)
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- 정렬 토글(추천순/최신순)은 **첫 번째 카드를 포함한 목록 전체**에 적용된다 — 상단에 고정되는 대표 카드 없음.
- "필요없는 정보들"은 스펙(계열·노트)을 부록으로 밀어내는 컨셉 선언 — 시향기가 주인공 (1단계 §1.1).
- 고정 바 탭 → 게스트면 로그인 바텀시트 → 이 향수가 선택된 채 `/write` (F3, D1).

### 2.8 시향기 상세 `/reviews/:slug` (B3 · M3)

```
┌──────────────────────────────┐
│ ←                   [⋯] [🔍] │ ← ⋯: 신고 (E3) /
├──────────────────────────────┤    본인 글이면 수정·삭제 (D2)
│ ┌──────────────────────────┐ │
│ │ ▢ 딥티크 · 오 로즈      › │ │ ← 향수 요약 카드 (→ 향수 상세)
│ └──────────────────────────┘ │
│                              │
│  “첫 출근날 긴장한 신입사원   │ ← 자유 서술 전문 — 이 화면의
│   같은 향이에요. 단정한데     │    주인공. 본문 16px+ (N7)
│   손끝은 떨리는.”            │
│                              │
│  이 사람의 프로필             │ ← 프로필 표 (§6-8)
│  ─────────────────────────  │
│  나이           20대         │
│  성격           차분한 · 순수한│
│  만나는 때       봄 · 출근    │
│  어울리는 사람   20~30대 ·    │
│                 중성적       │
│                              │
│  수줍은베르가못 › · 2026.8.9  │ ← 닉네임(→ 프로필) · 작성일
│                              │
│ ┌──────────────────────────┐ │
│ │        ♥ 추천 128         │ │ ← 1인 1추천, 재탭 시 취소,
│ └──────────────────────────┘ │    즉시 반영 (C2). 게스트 탭
│                              │    → 바텀시트 → 자동 반영 (F4)
│  (본인 글: “내가 소개한       │
│   사람이에요” — 비활성)       │
├──────────────────────────────┤
│  이 향수의 다른 페르소나 (11) │ ← 읽기 루프 F1의 귀환로
│ ┌──────────────────────────┐ │
│ │ 시향기 카드 (변형 B)       │ │ ← 10개씩 + [더보기] (§1.3)
│ └──────────────────────────┘ │
│         [ 더보기 ]           │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- URL 공유 시 로그인 없이 동일 화면 (B3, N6 — SEO 서버 렌더링 대상).
- 신고: ⋯ → 사유 선택 바텀시트 (허위 정보/비방·혐오/스팸·광고/기타) → 접수. 게스트도 가능, 동일 대상 중복 제한 (E3).

### 2.9 작성자 프로필 `/users/:id` (D4 · S2)

```
┌──────────────────────────────┐
│ ←                       [🔍] │
├──────────────────────────────┤
│  수줍은베르가못               │ ← 닉네임
│  소개한 사람 8 · 받은 추천 512 │ ← 시향기 수 · 총 추천 수
├──────────────────────────────┤
│ ┌──────────────────────────┐ │
│ │ 시향기 카드 (변형 A)       │ │ ← 최신순 · 10개씩 + [더보기]
│ └──────────────────────────┘ │
│         [ 더보기 ]           │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- 로그인 없이 열람 가능 (D4). 식별자 형식은 6단계에서 확정 (3단계 결정 5).

---

## 3. 로그인 바텀시트 (화면 아님 — 오버레이)

```
┌──────────────────────────────┐
│  (현재 화면이 뒤에 흐리게)     │
│                              │
│ ╭──────────────────────────╮ │
│ │            ──            │ │ ← 드래그 핸들 (내려서 닫기)
│ │                          │ │
│ │  이 사람을 소개하려면      │ │ ← 트리거별 문구 (아래 표)
│ │  3초면 충분해요           │ │
│ │                          │ │
│ │ ┌──────────────────────┐ │ │
│ │ │  카카오로 시작하기     │ │ │ ← 소셜 1탭 (공급자는 9단계
│ │ └──────────────────────┘ │ │    확정 — C1)
│ │                          │ │
│ │  로그인하면 하던 일로      │ │
│ │  바로 돌아가요            │ │ ← 복귀 보장 안내 (N2)
│ ╰──────────────────────────╯ │
└──────────────────────────────┘
```

| 트리거 | 헤드라인 문구 (예시 — 8단계에서 확정) |
|---|---|
| 쓰기 | 이 사람을 소개하려면 3초면 충분해요 |
| 추천 | 이 사람에게 추천을 보내려면 로그인이 필요해요 |
| MY | 내가 소개한 사람들을 보려면 로그인하세요 |

- 독립 페이지 없음. 콜백 라우트 `/auth/callback`만 존재 (3단계 결정 3).
- 최초 로그인 시 닉네임 자동 생성 — 생성 규칙은 6단계 (C1, 2단계 미해결 3).

---

## 4. 멤버 전용 화면 (2)

### 4.1 시향기 작성·수정 `/write` (D1 · D2 · M5)

한 페이지 스크롤 폼 (ADR 0004). 위저드 아님, 스텝 이동 없음.

```
┌──────────────────────────────┐
│ ✕  시향기 쓰기     ✓ 임시저장됨│ ← 자동 임시저장 상태 (D1)
├──────────────────────────────┤
│ ┌──────────────────────────┐ │
│ │ ▢ 딥티크 · 오 로즈  [변경] │ │ ← 향수 선택 칩
│ └──────────────────────────┘ │
│  ⓘ 실제 인물 비방·허위 정보는 │ ← 커뮤니티 가이드 1줄 (N9)
│    삭제됩니다                │
├──────────────────────────────┤
│  이 향수가 사람이라면,        │
│  어떤 사람인지 소개해 주세요   │
│                              │
│ ┌──────────────────────────┐ │
│ │ 첫 출근날 긴장한 신입사원  │ │ ← 비어 있으면 회전 예시
│ │ 같은…                    │ │    placeholder (D1)
│ │                    23/100│ │ ← 남은 글자 수, 100자 초과
│ └──────────────────────────┘ │    입력 제한 (D1)
│        [ 🃏 질문 카드 ]       │ ← 입력칸 바로 아래 (§6-11)
│  ┌─ 🃏 “이 사람의 MBTI는?” ─┐│ ← 버튼 탭 시 그 아래 말풍선에
│  └─────────────────────────┘│    랜덤 유도 질문 (D1)
├──────────────────────────────┤
│  이 사람의 프로필             │
│                              │
│  몇 살쯤일까요? · 1개         │
│  [10대][20대][30대][40대][50+]│
│                              │
│  어떤 분위기의 사람인가요?     │
│  · 1~3개                     │
│  [차분한]     [장난기 많은]   │ ← 12개 세트 (§6-6)
│  [도도한]     [다정한]        │
│  [신비로운]   [시크한]        │
│  [순수한]     [자유로운]      │
│  [섹시한]     [지적인]        │
│  [상큼발랄한] [우아한]        │
│                              │
│  이 사람을 언제 만날 것        │
│  같나요? · 1개 이상           │
│  [봄][여름][가을][겨울]       │
│  [데일리][데이트][출근]       │
│  [특별한 날]                 │
│                              │
│  누구에게 잘 어울릴까요?       │
│  · 1개 이상                  │
│  [10대][20대][30대][40대][50+]│ ← 연령대 × 성별 이미지
│  [여성적][남성적][중성적]      │    두 축 (§6-7)
├──────────────────────────────┤
│ ┃ 내가 아는 오 로즈는 이래요  ┃ │ ← 하단 고정 (§0 컨셉 언어,
└──────────────────────────────┘    조사 은/는 자동 처리).
                                    필수 미충족 시 비활성 + 탭하면
                                    부족한 항목으로 스크롤 이동
```

- **필수**: 자유 서술 1~100자 + 태그 질문 4종 각 최소 1개 (D1). 태그는 전부 탭만으로 응답 — 타이핑 없음.
- **진입 경로별 초기 상태**:
  - 향수 상세에서 진입 → 그 향수가 선택된 채 시작 (D1).
  - 쓰기 탭 직접 진입 → 향수 칩 자리가 향수 검색 필드로 시작 — 검색·선택 후 폼 활성화 (F3).
- **수정 모드** (D2): 같은 폼에 기존 값 채움. 향수 칩의 [변경] 미노출 — 수정 시 향수는 변경 불가.
- 등록 완료 → 시향기 상세로 이동, 피드·향수 상세에 즉시 노출 (D1).
- 이탈 시 임시저장 유지, 재진입 시 복원. 로그인 게이트를 거쳐도 작성 내용 보존 (N2·N3).

### 4.2 MY `/me` (D3 · M8)

```
─ 멤버 상태 ─
┌──────────────────────────────┐
│ MY                      [🔍] │
├──────────────────────────────┤
│  수줍은베르가못         [✎]  │ ← 닉네임 · 변경 (C1)
│                              │
│ ┌──────────────────────────┐ │
│ │   받은 추천        ♥ 512  │ │ ← 총 받은 추천 수 — 보상
│ └──────────────────────────┘ │    장치의 중심 (M8, D3)
├──────────────────────────────┤
│  내가 소개한 사람들 (8)       │
│ ┌──────────────────────────┐ │
│ │ 시향기 카드 (변형 A)       │ │ ← 최신순. 카드마다 글별
│ │              [수정][삭제] │ │    추천 수 + 수정·삭제 (D2)
│ └──────────────────────────┘ │
│             ⋮                │
│                              │
│  로그아웃                    │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘

─ 게스트 상태 ─
┌──────────────────────────────┐
│ MY                      [🔍] │
├──────────────────────────────┤
│                              │
│   내가 소개한 사람들과        │
│   받은 추천을 볼 수 있어요    │
│                              │
│  ┌────────────────────────┐ │
│  │      로그인하기         │ │ ← 탭 → 로그인 바텀시트 (§3)
│  └────────────────────────┘ │
├──────────────────────────────┤
│   홈      ( ✍ 쓰기 )    MY   │
└──────────────────────────────┘
```

- 삭제는 확인 다이얼로그를 거친다 (D2).

---

## 5. 관리자 화면 (3 — 데스크톱 우선, 별도 URL 직접 진입)

일반 내비게이션과 연결되지 않는다. 권한 플래그 검사 후 진입 (E1). 좌측 공통 메뉴로 3개 화면 간 이동.

### 5.1 시향기 관리 `/admin/reviews` (E1 · M9)

```
┌──────────┬───────────────────────────────────────────┐
│ 시향기    │ 시향기 관리                                │
│ 카탈로그  │ ┌───────────────┐ [최신순 ▾]              │
│ 신고      │ │ 🔍 작성자·향수  │                        │
│          │ └───────────────┘                         │
│          │ ┌─────────────────────────────────────┐   │
│          │ │ 작성일  작성자   향수    서술(요약)   │   │
│          │ │ 신고수  ♥   [보기][수정][삭제]        │   │
│          │ │ ──────────────────────────────────  │   │
│          │ │ 8.9   수줍은…  오 로즈  “첫 출근날…”  │   │
│          │ │ 0    128   [보기][수정][삭제]        │   │
│          │ │             ⋮                       │   │
│          │ └─────────────────────────────────────┘   │
└──────────┴───────────────────────────────────────────┘
```

- 삭제 시 **사유 메모 입력 모달** — 관리자 삭제는 일반 삭제와 구분 기록 (E1).
- 수정은 멤버 작성 폼과 동일한 폼을 재사용.

### 5.2 향수·브랜드 관리 `/admin/catalog` (E2 · S4)

```
┌──────────┬───────────────────────────────────────────┐
│ 시향기    │ 카탈로그 관리      [브랜드] [향수]  ← 탭    │
│ 카탈로그  │ ┌───────────────┐        [+ 향수 등록]     │
│ 신고      │ │ 🔍 검색        │                        │
│          │ └───────────────┘                         │
│          │ ┌─────────────────────────────────────┐   │
│          │ │ 이미지 향수명   브랜드  계열  시향기수 │   │
│          │ │ [수정][삭제]                         │   │
│          │ │ ──────────────────────────────────  │   │
│          │ │ ▢   오 로즈   딥티크  플로럴   12    │   │
│          │ │ [수정][삭제]                         │   │
│          │ │             ⋮                       │   │
│          │ └─────────────────────────────────────┘   │
└──────────┴───────────────────────────────────────────┘
```

- 등록/수정은 모달 폼 — 브랜드(이름·로고), 향수(이름·브랜드·이미지·계열·노트) (E2).
- **시향기가 달린 향수 삭제 시 경고 모달** — 시향기 수를 명시하고 재확인 (E2).
- 초기 시딩(인기 향수 100~300개)은 이 화면 또는 스크립트로 (E2, 1단계 §4.1).

### 5.3 신고 관리 `/admin/reports` (E3 · S3)

```
┌──────────┬───────────────────────────────────────────┐
│ 시향기    │ 신고 관리          [미처리 ▾]              │
│ 카탈로그  │ ┌─────────────────────────────────────┐   │
│ 신고      │ │ 신고일  대상 시향기   사유    누적    │   │
│          │ │ 상태   [삭제 처리][무시]              │   │
│          │ │ ──────────────────────────────────  │   │
│          │ │ 8.10  “첫 출근날…”  비방·혐오  3     │   │
│          │ │ 미처리 [삭제 처리][무시]              │   │
│          │ │             ⋮                       │   │
│          │ └─────────────────────────────────────┘   │
└──────────┴───────────────────────────────────────────┘
```

- 대상 시향기 클릭 → 시향기 상세(관리자도 동일 화면으로 열람). 삭제 처리는 5.1의 사유 메모 흐름과 동일.

---

## 6. 이번 단계에서 확정된 결정 (2026-08-14 사용자 승인)

1. **상단 탭 라벨**: 피드 / **브랜드로 찾기** / **계열로 찾기** — "찾기"를 라벨에 노출해 탐색 탭임을 즉시 전달.
2. **홈 피드는 추천순 단일 정렬** — 최신순 토글 없음. ⚠️ 2단계 B1의 "최신순 전환 가능"을 일부 변경하는 결정 (사용자 확정). 향수 상세의 목록(B2)에는 추천순/최신순 토글 유지.
3. **시향기 카드는 브랜드·향수명이 맨 위** — 자유 서술보다 먼저. 같은 향수 문맥(향수 상세·"이 향수의 다른 페르소나")에서는 이 줄을 생략한 변형 B 사용.
4. **향수 상세의 스펙 정보는 "필요없는 정보들" 접힘 버튼 뒤로** — 계열 칩·노트·외부 구매 링크 슬롯(N12)을 펼침 영역에 수납. 버튼 바로 아래에 시향기 목록 시작.
5. **"시향기 쓰기"는 하단 고정 바** — 향수 상세에서 스크롤과 무관하게 상시 노출 (B2 요건 충족 방식 확정).
6. **성격 태그 12개 세트** — 차분한·장난기 많은·도도한·다정한·신비로운·시크한·순수한·자유로운·섹시한·지적인·상큼발랄한·우아한 (다중 선택, 최대 3개).
7. **"어울리는 사람"은 연령대 × 성별 이미지 두 축** — 연령대 5구간(10대~50대+) + 성별 이미지 3종(여성적/남성적/중성적), 각각 복수 선택.
8. **시향기 상세의 태그 표시는 "이 사람의 프로필" 표 형태** — 질문별 항목명·답을 줄맞춰 인물 소개 카드처럼.
9. **컨셉 언어 확정 (§0 표)** — 시향기 목록 제목 "{향수 이름}(이)가 사람이라면", 하단 목록 "이 향수의 다른 페르소나", 등록 버튼 "내가 아는 {향수 이름}은/는 이래요", 빈 상태 "이 향수에 영혼을 불어넣어 사람으로 만들어주세요".
10. **시향기 목록은 10개씩 + [더보기] 버튼** — 무한 스크롤 아님. 시향기 카드가 나오는 모든 목록에 공통 적용 (B1의 "무한 스크롤 또는 더보기" 중 더보기로 확정).
11. **질문 카드 버튼과 유도 질문 말풍선은 자유 서술 입력칸 바로 아래** — 입력하다 막힌 시점의 시선 위치에 배치. 태그 질문 4종은 각 최소 1개 필수 유지 (D1 그대로 — 논의 후 원안 유지로 재확정).

## 7. 다음 단계로 넘기는 것

| 항목 | 다룰 단계 |
|---|---|
| 각 화면의 데이터 항목 정리 | 5단계 (이 문서의 화면별 표시 요소가 입력이 된다) |
| 슬러그 생성 규칙·프로필 식별자 형식 | 6단계 ERD (3단계 결정 5) |
| 계열 목록의 최종 세트 (통제 어휘) | 6단계 ERD와 함께 (ADR 0003) |
| 닉네임 자동 생성 규칙·중복 처리 | 6단계 ERD |
| 질문 카드 질문 풀 (20~30개) · placeholder 회전 예시 풀 | 8단계 전까지 |
| 색·타이포·아이콘·간격, 바텀시트 문구 확정, 쓰기 탭 강조 형태 | 8단계 UI 상세 |
| 소셜 로그인 공급자 확정 (카카오 우선) | 9단계 |
